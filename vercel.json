{
  "framework": "nextjs",
  "buildCommand": "npm run db:generate && npm run build",
  "devCommand": "npm run dev",
  "installCommand": "npm ci",
  "regions": ["fra1"],
  "env": {
    "NODE_ENV": "production"
  }
}
